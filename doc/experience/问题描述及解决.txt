问题描述：
如图xtal_unusual_pic.jpg所示，图中绿色为时钟线，图中的异常时时钟在起振过程中存在被打断的过程。这个异常的原因未知。

问题解决方法：
程序中采用了IAP编程，其中对于时钟的初始化在BOOT中进行了一次，在进入APP后又进行了一次，在APP中进行RCC设置时对所有的RCC寄存器进行了复位操作，这个操作导致了图中异常的出现，在去掉对RCC寄存器的复位操作后，时钟起振过程恢复正常，如图xtal_usual_pic.jpg所示。